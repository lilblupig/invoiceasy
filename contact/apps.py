""" App config information for contact page """

from django.apps import AppConfig


class ContactConfig(AppConfig):
    """ App config for Contact app """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'contact'
